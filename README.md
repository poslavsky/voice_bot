# Voice Note Bot 🎤📝

Telegram бот для транскрипции голосовых сообщений и форматирования в заметки.

**Стек:** Vercel Serverless + Gemini API

## Как это работает

1. Отправляешь голосовое сообщение боту
2. Бот возвращает:
   - **Транскрипция** — точный текст того, что ты сказал
   - **Заметка** — отформатированная версия без слов-паразитов, с абзацами

## Деплой (5 минут)

### 1. Создай бота в Telegram

1. Открой [@BotFather](https://t.me/botfather)
2. Отправь `/newbot`
3. Придумай имя и username
4. Сохрани токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Получи Gemini API ключ

1. Иди на [Google AI Studio](https://aistudio.google.com/apikey)
2. Нажми "Create API Key"
3. Сохрани ключ

### 3. Задеплой на Vercel

**Вариант A: Через GitHub (рекомендуется)**

```bash
# Создай репозиторий и запушь код
git init
git add .
git commit -m "Initial commit"
gh repo create voice-note-bot --private --push
```

**Важное примечание:** В проекте создан файл `.env`, содержащий ваш `BOT_TOKEN`. Не забудьте также добавить `GEMINI_API_KEY` в этот файл или в настройки вашего хостинга.

Потом:
1. Иди на [vercel.com/new](https://vercel.com/new)
2. Импортируй репозиторий
3. В настройках добавь Environment Variables:
   - `BOT_TOKEN` = твой токен от BotFather
   - `GEMINI_API_KEY` = твой ключ от Google AI Studio
4. Нажми Deploy

**Вариант B: Через CLI**

```bash
npm i -g vercel
vercel login
vercel --prod
```

При деплое Vercel спросит про environment variables — добавь их.

### 4. Установи webhook

После деплоя у тебя будет URL типа `https://voice-note-bot.vercel.app`

Открой в браузере (замени на свои данные):

```
https://api.telegram.org/bot<BOT_TOKEN>/setWebhook?url=https://<твой-проект>.vercel.app/api/webhook
```

Должен вернуться ответ: `{"ok":true,"result":true,"description":"Webhook was set"}`

### 5. Готово!

Открой бота в Telegram и отправь голосовое сообщение.

## Стоимость

- **Vercel**: бесплатно (100 GB-hours/месяц)
- **Gemini API**: бесплатно до 20 запросов/день, потом ~$0.30 за миллион токенов

Для личного использования — полностью бесплатно.

## Структура проекта

```
voice-note-bot/
├── api/
│   └── webhook.js    # Основная логика бота
├── package.json
└── README.md
```

## Кастомизация

Хочешь изменить формат заметки? Отредактируй промпт в функции `formatAsNote()` в `api/webhook.js`.

## Troubleshooting

**Бот не отвечает:**
- Проверь логи в Vercel Dashboard → твой проект → Logs
- Убедись что webhook установлен правильно

**Ошибка Gemini:**
- Проверь что API ключ правильный
- Возможно, превышен лимит бесплатных запросов

**Таймаут на длинных голосовых:**
- Vercel бесплатный тир имеет лимит 10 секунд
- Для голосовых >3 минут может не хватить времени
